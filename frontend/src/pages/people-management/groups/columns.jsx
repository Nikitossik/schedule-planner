export { useGroupColumns } from "@/hooks/useGroupColumns";

// Export the hook as default for backward compatibility
import { useGroupColumns } from "@/hooks/useGroupColumns";
export const columns = useGroupColumns;
