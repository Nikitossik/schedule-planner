export { useWorkloadColumns } from "@/hooks/useWorkloadColumns";

// Export the hook as default for backward compatibility
import { useWorkloadColumns } from "@/hooks/useWorkloadColumns";
export const columns = useWorkloadColumns;
