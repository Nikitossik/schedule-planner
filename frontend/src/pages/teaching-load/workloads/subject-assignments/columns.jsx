export { useSubjectAssignmentColumns } from "@/hooks/useSubjectAssignmentColumns";

// Export the hook as default for backward compatibility
import { useSubjectAssignmentColumns } from "@/hooks/useSubjectAssignmentColumns";
export const columns = useSubjectAssignmentColumns;
